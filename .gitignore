# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Migration artefacts containing real personal data (parent phones/emails,
# child health conditions). Never commit these.
migration/02_data.sql
migration/*.backup
.vercel
# vercel link writes this; it holds the project's OIDC token.
# Narrower than the ".env*" the CLI adds, which would also ignore
# .env.example — the one env file that is meant to be committed.
.env.local
